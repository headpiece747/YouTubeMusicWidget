<UserControl x:Class="YouTubeMusicWidget.YouTubeMusicWidgetSettings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="150" d:DesignWidth="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Authentication Status:" FontWeight="Bold"/>
        <Label Grid.Row="1" x:Name="AuthStatusLabel" Content="Not Authenticated" Margin="0,5,0,10"/>

        <Button Grid.Row="2" x:Name="AuthenticateButton" Content="Authenticate" Click="AuthenticateButton_Click" HorizontalAlignment="Center"/>

    </Grid>
</UserControl>
